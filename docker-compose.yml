---
version: '0.8'
services:
  app:
    build:
      context: .
      dockerfile: web-director.Dockerfile
    container_name: "web-director"
    image: "haproxy:2.3"
    restart: unless-stopped
    ports:
      - '80:80'
      - '443:443'
